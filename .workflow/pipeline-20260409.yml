version: '1.0'
name: pipeline-20260409
displayName: pipeline-20260409
triggers:
  trigger: auto
  push:
    tags:
      prefix:
        - v*
stages:
  - name: stage-24488044
    displayName: 未命名
    strategy: naturally
    trigger: auto
    executor: []
    steps:
      - step: build@rust
        name: build_rust
        displayName: Rust 构建
        rustVersion: '1.79'
        commands:
          - cargo --version
        artifacts:
          - name: BUILD_ARTIFACT
            path:
              - ./target
        caches: []
        notify: []
        strategy:
          retry: '0'
  - name: stage-c6daa8d4
    displayName: 未命名
    strategy: naturally
    trigger: auto
    executor: []
    steps:
      - step: build@nodejs
        name: build_nodejs
        displayName: Nodejs 构建
        nodeVersion: 24.5.0
        commands:
          - '# 设置NPM源，提升安装速度'
          - npm config set registry https://registry.npmmirror.com
          - ''
          - '# 执行编译命令'
          - npm install && npm run tauri build
        artifacts:
          - name: BUILD_ARTIFACT
            path:
              - ./dist
        caches:
          - ~/.npm
          - ~/.yarn
        notify: []
        strategy:
          retry: '0'
